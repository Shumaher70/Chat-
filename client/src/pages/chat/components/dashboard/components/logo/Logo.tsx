import styles from './Logo.module.scss';
const Logo = () => {
   return (
      <div className={`${styles.logo} dark-1`}>
         <p>Chat-</p>
         <div></div>
      </div>
   );
};

export default Logo;
